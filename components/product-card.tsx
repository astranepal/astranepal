import Link from "next/link"; import Image from "next/image"; import type {Product} from "@/lib/types"; import {formatNpr} from "@/lib/utils";
export function ProductCard({product}:{product:Product}){return <Link href={"/products/"+product.slug} className="card"><div className="card-image">{product.images[0]&&<Image src={product.images[0]} alt={product.name} fill sizes="(max-width:580px) 50vw,(max-width:900px) 33vw,25vw"/>}{product.discount>0&&<span className="badge">-{product.discount}%</span>}</div><div className="card-info"><div className="card-top"><span className="card-name">{product.name}</span><span>★ {Number(product.rating).toFixed(1)}</span></div><div className="price">{formatNpr(product.price)}{product.original_price&&<span className="old">{formatNpr(product.original_price)}</span>}</div><div className="meta">{product.category?.name} · {product.collection?.name}</div></div></Link>}