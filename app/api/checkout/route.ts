import {NextResponse} from "next/server"; import {createClient} from "@/lib/supabase/server";
export async function POST(req:Request){try{const body=await req.json();const supabase=await createClient();const {data,error}=await supabase.rpc("place_order",{p_customer_name:body.customer_name,p_email:body.email,p_phone:body.phone,p_province:body.province,p_city:body.city,p_delivery_address:body.delivery_address,p_delivery_notes:body.delivery_notes??"",p_items:body.items,p_discount_code:body.discount_code??null});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json(data);}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Invalid checkout request"},{status:400});}}