import {NextResponse} from "next/server"; import {createClient} from "@/lib/supabase/server"; export async function POST(req:Request){try{const b=await req.json();const s=await createClient();const {data,error}=await s.rpc("save_contact_message",{p_name:b.name,p_email:b.email,p_phone:b.phone??"",p_message:b.message});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({id:data});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Invalid request"},{status:400});}}