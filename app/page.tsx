import Link from "next/link"; import Image from "next/image"; import {getFeaturedProducts,getCollections} from "@/lib/data"; import {ProductCard} from "@/components/product-card";
export default async function Home(){const [products,collections]=await Promise.all([getFeaturedProducts(),getCollections()]);return <main><section className="hero"><Image className="hero-image" src="https://images.unsplash.com/photo-1529139574466-a303027c1d8b?auto=format&fit=crop&w=2400&q=86" alt="ASTRA MEN" fill priority sizes="100vw"/><div className="hero-overlay"/><div className="hero-copy"><span className="eyebrow light">ASTRA MEN · 2026 EDIT</span><h1>Clothes for the pace of the city.</h1><p>Quietly confident menswear. Clean cuts, durable fabrics, no unnecessary noise.</p><div className="actions"><Link className="button light-button" href="/products">Shop the edit</Link><Link className="text-link light" href="/products?collection=new-arrivals">New arrivals →</Link></div></div></section><section className="section"><div className="section-heading"><div><span className="eyebrow">The edit</span><h2>Essential pieces, considered.</h2></div><Link className="text-link" href="/products">View all →</Link></div><div className="product-grid">{products.slice(0,4).map(p=><ProductCard key={p.id} product={p}/>)}</div></section><section className="collection-grid">{collections.slice(0,4).map(c=><Link href={"/products?collection="+c.slug} className="collection-tile" key={c.id}>{c.image&&<Image src={c.image} alt="" fill sizes="50vw"/>}<div className="tile-copy"><span>{c.name}</span><small>Explore collection →</small></div></Link>)}</section><section className="quote-section"><span className="eyebrow">Our approach</span><blockquote>Dress simply. Move deliberately. Keep what earns its place.</blockquote><p>Designed in Kathmandu with a focus on fit, fabric and repeat wear.</p></section></main>}