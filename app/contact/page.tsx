"use client"; import {FormEvent,useState} from "react"; export default function Contact(){const [done,setDone]=useState(false);const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const f=new FormData(e.currentTarget);const r=await fetch("/api/contact",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name:f.get("name"),email:f.get("email"),phone:f.get("phone"),message:f.get("message")})});const d=await r.json();if(!r.ok)setError(d.error||"Could not send message");else setDone(true);setLoading(false)}return <main className="account-page"><div style={{maxWidth:760,margin:"30px auto"}}><span className="eyebrow">Contact</span><h1 style={{fontFamily:"var(--display)",fontSize:56,letterSpacing:"-.04em"}}>Let’s talk.</h1><p className="section-subtitle">Questions about sizing, orders or returns? Send a message and our team can follow up. Business contact details below are placeholders for your final brand configuration.</p>{done?<div className="notice" style={{color:"#215d3c",background:"#eefaf3",borderColor:"#cdebd8"}}>Message sent. Thank you.</div>:<form onSubmit={submit}><div className="form-grid"><label className="field"><span>Name</span><input name="name" required/></label><label className="field"><span>Email</span><input name="email" type="email" required/></label><label className="field"><span>Phone</span><input name="phone"/></label><label className="field full-span"><span>Message</span><textarea name="message" rows={6} required minLength={10}/></label></div>{error&&<div className="notice" style={{marginTop:14}}>{error}</div>}<div style={{marginTop:18}}><button className="button blue" disabled={loading}>{loading?"Sending…":"Send message"}</button></div></form>}<div className="info-card" style={{marginTop:28}}><strong>Placeholder contact</strong><p className="meta">Email: hello@example.com · Phone: +977 98XXXXXXXX · Hours: Mon–Sat, 10:00–18:00 NPT</p></div></div></main>}