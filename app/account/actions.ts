"use server"; import {createClient} from "@/lib/supabase/server"; import {redirect} from "next/navigation";
export async function signOut(){const s=await createClient();await s.auth.signOut();redirect("/");}
export async function saveProfile(formData:FormData){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/login");const payload={id:user.id,full_name:String(formData.get("full_name")||""),phone:String(formData.get("phone")||""),province:String(formData.get("province")||""),city:String(formData.get("city")||""),delivery_address:String(formData.get("delivery_address")||""),delivery_notes:String(formData.get("delivery_notes")||"")};const {error}=await s.from("profiles").upsert(payload);if(error)throw new Error(error.message);redirect("/account?updated=1");}