import {redirect} from "next/navigation"; import {createClient} from "@/lib/supabase/server"; import {signOut} from "./actions";
export default async function Account(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/login");const {data:orders}=await s.from("orders").select("id,total,status,created_at").order("created_at",{ascending:false}).limit(20);return <main className="account-page"><span className="eyebrow">Account</span><h1 style={{fontFamily:"var(--serif)",fontSize:56}}>Hello, {user.email}</h1><form action={signOut}><button className="button">Sign out</button></form><section style={{marginTop:60}}><h2 style={{fontFamily:"var(--serif)",fontSize:38}}>Order history</h2>{orders?.length?<div>{orders.map(o=><div className="cart-item" key={o.id}><div><strong>{o.id}</strong><div className="meta">{new Date(o.created_at).toLocaleDateString()} · {o.status}</div></div><strong>{o.total}</strong></div>)}</div>:<div className="empty">No orders yet.</div>}</section></main>}